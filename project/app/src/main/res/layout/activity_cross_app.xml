<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/ic_cross_app_bg">

    <!-- 基础组件 -->
    <include layout="@layout/include_scene_content" />

    <tech.sud.mgp.hello.ui.scenes.crossapp.widget.view.CrossAppStatusView
        android:id="@+id/cross_app_status_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/room_mic_view"
        app:layout_constraintBottom_toTopOf="@id/room_chat_view" />

</androidx.constraintlayout.widget.ConstraintLayout>