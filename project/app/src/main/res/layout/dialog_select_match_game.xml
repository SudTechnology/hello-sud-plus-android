<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/shape_white_top_round"
    android:paddingBottom="12dp">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:clipToPadding="false"
        android:layout_marginHorizontal="10dp"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="38dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/container_operate" />

    <LinearLayout
        android:id="@+id/container_operate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        app:layout_constraintBottom_toBottomOf="parent">

        <TextView
            android:id="@+id/tv_single_match"
            android:layout_width="162dp"
            android:layout_height="44dp"
            android:textSize="16sp"
            android:textColor="@color/white"
            android:gravity="center"
            android:paddingHorizontal="2dp"
            android:text="@string/single_fast_match"
            android:background="@color/black" />

        <TextView
            android:id="@+id/tv_team_match"
            android:layout_width="162dp"
            android:layout_height="44dp"
            android:textSize="16sp"
            android:textColor="@color/white"
            android:gravity="center"
            android:paddingHorizontal="2dp"
            android:text="@string/team_match"
            android:background="@color/black"
            android:layout_marginStart="19dp" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>