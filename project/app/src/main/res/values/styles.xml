<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 从底部弹出的动画 -->
    <style name="BottomToTopAnim" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/audio_bottomview_anim_enter</item>
        <item name="android:windowExitAnimation">@anim/audio_bottomview_anim_exit</item>
    </style>

    <!-- 从顶部滑出的动画 -->
    <style name="TopToBottomAnim" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/topview_anim_enter</item>
        <item name="android:windowExitAnimation">@anim/topview_anim_exit</item>
    </style>

    <!-- 透明度动画 -->
    <style name="AudioFadeAnim" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/audio_fade_in</item>
        <item name="android:windowExitAnimation">@anim/audio_fade_out</item>
    </style>

    <style name="audio_dialog_soft_input" parent="Theme.AppCompat.Dialog">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowSoftInputMode">adjustNothing</item>
    </style>

    <style name="LightStatusBarDialogTheme" parent="Theme.MaterialComponents.Dialog">
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>